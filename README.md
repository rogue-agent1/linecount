# linecount
Fast line/word/byte counter (wc alternative) with recursive support.
```bash
python linecount.py *.py
python linecount.py -r src/ -e py js -l
cat file.txt | python linecount.py
```
## Zero dependencies. Python 3.6+.
